## Exemplo de configuração das variáveis de ambiente
# Copie este arquivo para ".env" na raiz e configure os valores. NÃO versione o arquivo .env real.

# Servidor
PORT=5010
NODE_ENV=production

# MongoDB (use o URI gerenciado da DO ou Atlas)
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority

# Segurança
JWT_SECRET=troque_este_segredo

# Logs
LOG_LEVEL=info

# IA
IA_CONFIDENCE_THRESHOLD=0.7

# URL do Frontend (CORS e e-mails)
FRONTEND_URL=https://ia.nexarhub.com.br

# SMTP (Hostinger ou seu provedor)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Nexar.ia <contato@nexarhub.com.br>

# Puppeteer/Chromium para whatsapp-web.js (se estiver usando whatsapp-web.js)
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
PUPPETEER_ARGS=--no-sandbox --disable-setuid-sandbox
WA_HEADLESS=true

# --- Migração de dados (opcional) ---
# Banco de origem (ex.: local)
SOURCE_MONGO_URI=mongodb://localhost:27017/saas-ia-whatsapp
# Nome do DB de origem (opcional; se omitido, usa o da URI)
SOURCE_DB_NAME=
# Banco de destino (Atlas)
DEST_MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority&appName=NexarIA
# Nome do DB de destino (opcional; se omitido, usa o da URI)
DEST_DB_NAME=
# Define se apaga os dados do destino antes de migrar (0|1)
DROP_DEST=0
