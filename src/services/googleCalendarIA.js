// Funções para IA consultar e criar eventos na agenda Google do cliente
const Client = require('../models/Client');
const googleCalendarService = require('./googleCalendarService');

module.exports = {
  // Buscar horários disponíveis (exemplo: próximos 7 dias, 8h-18h)

// Arquivo removido: IA Google Calendar desativada
};
